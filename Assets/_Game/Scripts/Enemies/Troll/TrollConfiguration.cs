public class TrollConfiguration : IEnemyConfiguration {

    public TrollConfiguration(short life, float stunTime, short speed) : base(life, stunTime, speed) {
    }

    // used to define the configuration of the troll

}